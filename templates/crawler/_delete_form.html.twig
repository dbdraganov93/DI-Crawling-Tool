<form method="post" action="{{ path('app_crawler_delete', {'id': crawler.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crawler.id) }}">
    <button  class="btn btn-sm btn-danger" style="display: inline;">Delete</button>
</form>
