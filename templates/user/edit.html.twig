{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="center-align">Edit User</h1>

        <div class="row">
            <div class="col s12">
                {{ include('user/_form.html.twig', {'button_label': 'Update', 'attr': {'class': 'card-panel'}}) }}
            </div>
        </div>

        <div class="right-align mt-4">
            <a href="{{ path('app_user_index') }}" class="waves-effect waves-light btn">Back to List</a>
            {{ include('user/_delete_form.html.twig', {'id': user.id}) }}
        </div>
    </div>
{% endblock %}
