<?php

class Marktjagd_Database_Mapper_CompaniesWithAdMaterial extends Marktjagd_Database_Mapper_Abstract
{
    /**
     * Returns the database table class, if no one exists,
     * default will be created.
     *
     * @return  Marktjagd_Database_DbTable_CompaniesWithAdMaterial
     */
    public function getDbTable()
    {
        return parent::getDbTable();
    }

    /**
     * Saves data to database. If the primary key is set,
     * data will be updated.
     *
     * @param Marktjagd_Database_Entity_CompaniesWithAdMaterial $oCompaniesWithAdMaterial Object data
     * @param bool $bNull Save also null values
     * @param bool $bForceInsert Force insert
     * @return void
     */
    public function save(Marktjagd_Database_Entity_CompaniesWithAdMaterial $oCompaniesWithAdMaterial, $bNull = false, $bForceInsert = false)
    {
        parent::_save($oCompaniesWithAdMaterial, $bNull, $bForceInsert);
    }

    /**
     * Loads data by primary key(s). By multiple primary
     * keys use an array with the values of the primary key columns.
     *
     * @param mixed $mId Primary key(s) value(s)
     * @param Marktjagd_Database_Entity_CompaniesWithAdMaterial  $oCompaniesWithAdMaterial Object for data
     *
     * @return bool True if found, otherwise false
     */
    public function find($mId, Marktjagd_Database_Entity_CompaniesWithAdMaterial $oCompaniesWithAdMaterial)
    {
        return parent::_find($mId, $oCompaniesWithAdMaterial);
    }
    
    /**
     * 
     * @param type $startDate
     * @param type $endDate
     * @param type $idCompany
     * @param Marktjagd_Database_Collection_CompaniesWithAdMaterial $oCompaniesWithAdMaterial
     * @return boolean
     */
    public function findCompletenessByTimeSpan($startDate, $endDate, $idCompany, Marktjagd_Database_Collection_CompaniesWithAdMaterial $oCompaniesWithAdMaterial) {
        
        $result = $this->getDbTable()->findCompletenessByTimeSpan($startDate, $endDate, $idCompany);
        
        if (count($result) > 0) {
            $oCompaniesWithAdMaterial->setOptions($result);
            return true;
        }

        return false;
    }
}
